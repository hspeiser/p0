# main repo
To run, install [uv](https://docs.astral.sh/uv/getting-started/installation/), and run any entry points, such as `src/main.py` or the scripts in `utility/`. 

## main.py
main.py has the option to connect to a grpc server, het uses this rn to connect to his windows cause wsl dookie
